function es = esatwat2(t)%ESATWAT2 			 vapor pressure over water -- only water%  using the technique from NCAR (Eric Miller)%				es = esatwat(t)% Input: %  t degC --  column vector.%Output: %  es (hPa) -- a column vector%%Reynolds, 970220% test: t = [0; 10; 20; 30];  ==>  42.3731a = 5.0065;b = 19.83823;e3 = 6.1078;t3 = 273.15;tk = t + t3 .* ones(size(t));  % degKes = e3 * exp(a .* log(t3 ./ tk)) .* ...	exp((a + b) .* (1.* ones(size(t)) - t3 ./ tk));  return